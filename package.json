{
  "name": "sass-css-starter",
  "version": "1.0.0",
  "description": "Starter project for SCSS to CSS compilation",
  "scripts": {
    "build": "gulp build",
    "dev": "gulp watch"
  },
  "devDependencies": {
    "gulp": "^5.0.0",
    "gulp-clean-css": "^4.3.0",
    "sass": "^1.69.0"
  }
}
